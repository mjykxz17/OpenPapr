# STATE — OpenPapr

> 2026-09-13 自动起草。来源=hidden memory 文件 + 本项目 `.claude/PROJECT-HISTORY.md`(由历史 transcript 生成)，
> 每条都带日期；没有来源的一律不写，需要 Aiden 确认的标 `[待确认]`。
> 压缩会吃掉 tool 输出，所以确切路径/PID/端口/数字口径写在这里，收工时覆盖更新。

## 现在在做什么
- 09-23 (Cowork)：**备用 AI 提供商 + 每分钟请求数(RPM)排队**，commit `e9f1f86` → **v37 complete**，健康检查 passing。迁移 0019：users 加 `llm_rpm`、`llm_fallback_base_url/model/key_enc/rpm`。`src/enrich/openai-compat.ts` 的 `complete()`：先主提供商，任何失败(宕机/超时/限流/没额度/key 失效)就换备用；主提供商连续失败 3 次后 3 分钟内直接跳过(有备用时)；429/503 带 Retry-After ≤60s 等一次再试同一家。`src/lib/rate-gate.ts`：按 baseUrl+key 的先到先得队列，1 分钟内不超过 N 次，超出的排队等待；排队时触发 worker 心跳(看门狗不误杀)。共享 key 可用 env `OPENAI_COMPAT_RPM`。只有备用没有主提供商时备用当主。指南 sourceNote 记录实际作答的模型。UI：Account → AI provider 两个表单(主/备用)，各带 "Requests per minute"(空=不限)。验证：466 单测(含真实时序的限流测试、备用切换、熔断、Retry-After)；真 worker + 两个 mock(主 500、备用 RPM)：主只被打 3 次后跳过，备用写完指南；第一次跑时备用 RPM=3 下 9 个请求排队到第 126 秒才轮到大纲，符合设计。
- 09-23 (Cowork)：**PDF 压缩**，commit `0b6daf0` → **v36 complete**。`src/server/pdf-optimize.ts`：Ghostscript `/printer`(300 dpi 图片重采样，文字仍是文字) + qpdf `--object-streams=disable`(每个对象独立偏移，PDF.js 按 Range 只取需要的部分)。**不做 linearize**：实测让 PDF.js 首页变慢(2.3s vs 1.3s)。IFS4103 Lect 1：22.7MB→3.4MB；20Mbit/s 下首页 ~1.35s(与原来持平)，跳到第 45 页 0.5s(原 2.8s)。进入缓存时做一次(下载 PDF / pptx 转换后 / deck.ts 的 cacheDeck)，结果需小 ≥10% 且页数一致才替换；`<pdf>.opt` 标记已处理(含"不值得"/失败)；串行，每次 ~50MB 内存 ~5s。worker 维护任务回填旧缓存(每轮 40 个)；LRU 淘汰时标记随 PDF 一起删。镜像加 `ghostscript qpdf`(线上 gs 10.07.1)。**线上首次回填：压缩 22 个 deck，省 65MB**(缓存原 105MB)。
- 09-23 (Cowork)：返回按钮改成看得见的按钮，commit `94ef997` → **v35 complete**，健康检查 passing。新组件 `src/components/BackLink.tsx`(36px 带边框、粗 chevron、目的地名称、hover 强调色、focus ring、读屏 "Back to …")，用在模块页(Home)、指南页(CODE overview)、文件查看器(CODE)；PDF 查看器翻页 ‹ › 也换成 chevron 图标。附带确认：本次部署关机时不再出现 `umounting /data: EBUSY`(新 start.sh 转发 SIGTERM 生效)。
- 09-23 (Cowork)：**可靠性全面检查**，commit `598785a` → **v34 complete**，Fly 健康检查 `/api/health` 1/1 passing。做了：云端 452 单测 + build + e2e 5/5 + 全站爬取(17 个路由 × 亮/暗/手机，无报错、无横向溢出，页面 0.5–1.0s) + 压测(40 个并发幻灯片渲染全 200、缓存后 150ms；繁忙写入进程并发 60 个 POST 全 200) + 线上日志/状态。**线上真 bug**：course_history 一直写不进(Canvas 对已关闭课程只回 `{id, access_restricted_by_date}`)，每 5 分钟报 NOT NULL 失败 → 已跳过这类课程，重启后不再出现。其他修复：SQLite `busy_timeout=10000` + `synchronous=NORMAL`；Canvas/Graph/LLM 全部加超时(以前一个挂死连接会让所有人停止同步)；start.sh 监管 worker(退出自动重启、SIGTERM 转发、优先 `node_modules/.bin/tsx`)，worker 20 分钟无进展看门狗退出、uncaughtException 退出；指南生成改为与 tick 并行，重启遗留的运行标记 "Interrupted"；幻灯片渲染最多 2 并发 + 同请求合并、同一 deck 只下载一次；Canvas 文件流式落盘(500MB 上限)；缓存原子写、1.5GB LRU 淘汰、清理 .part；sync_runs 加索引、7 天清理(**首次运行清掉 41,858 行**，缓存 82MB)；邮件同步先存新 refresh token、delta 过期自动重置；加 error/global-error/not-found 页；fly.toml 加 512MB swap。未做：deck-cache 按文件名 stem 做键，同名 pdf/pptx 会共用一个缓存；worker 仍用 tsx 运行(未打包)。
- 09-23 (Cowork)：修指南里 mermaid 报错"Syntax error in text"炸弹图堆在页面底部，commit `94f835e` → **v33 complete**。根因：mermaid.render 失败时把错误 SVG 画进它自己挂在 <body> 下的临时节点(`#dmmd-N`)，组件外面，每个坏图留一个。修法：`suppressErrorRendering`、先 `parse` 再 render、render 后清掉 body 里的残留(但不删组件内同 id 的成品 SVG)；新 `src/lib/mermaid-fix.ts` 用括号配对自动给含标点的标签加引号(生成时 `repairMermaidBlocks` + 显示时再试一次)；仍画不出的折叠成一行"show its source"。单测直接跑真 mermaid 解析器(444 通过)，浏览器验证 3 个图画出、1 个折叠、页面无炸弹。
- 09-23 (Cowork)：修"右上角一直显示 no LLM provider configured"，commit `862bbc9` → **v32 complete**。根因：指南按钮显示该模块**最新一次** guide_run 的错误，不管多久以前；那条是 v28 前（还没有每人自己的 key）留下的失败记录，旧文案，永远挂着。现在只在 15 分钟内结束、或本页点击发起的 run 才显示；已有 provider 时隐藏"no provider"类失败；可点 × 关闭。纯函数 `showOutcome` 有单测(435 通过)。
- 09-23 (Cowork)：**指南可选择用哪些文件生成**，commit `3c30d36` → **v31 complete**。"Generate study guide…/Regenerate…" 弹出选择器：模块内所有 PDF/office 文件按 Materials 分组，默认勾选讲义(含没有 PDF 同名的 pptx)，过滤/每组全选/Lecture slides·All·None；已有章节的文件标 "in the guide"；已有指南时默认"只重写这些章节，保留其余"(merge)，也可整本替换。迁移 0017：guide_runs 加 file_ids_json、mode。worker 通过 `ensurePdf` 走卷缓存(pptx 用 LibreOffice 转一次)，不再每次从 Canvas 重下；merge 用 `src/lib/guide-chapters.ts` 按章节引用最多的 deck 拆分、替换、按讲义自然序排序并重编号。修了带空格/括号的 deck 名引用：生成时百分号编码(`citeDeck`)，解析时解码。验证：430 单测、build、真 worker + mock LLM 端到端(先写 W4+pptx，再 merge W6+W4 → 3 章正确顺序，引用渲染成 chip)、只选图片 400、390px 弹层不溢出。
- 09-23 (Cowork)：**模块画像 + 学生画像 + 本周计划**，commit `2690f95` → **v30 complete**。迁移 0016：新表 course_history / nusmods_modules / nusmods_reviews / module_profiles / weekly_plans，users 加 major、study_year、style_learning、writing_style_*、profile_*。数据源：NUSMods API(按课程码全站共享缓存，7 天)、NUSMods 评论 = Disqus(forum `nusmods-prod`，thread:ident=课程码；**需要 Fly secret `DISQUS_API_KEY`，线上还没设 → 目前没有评论**)、Canvas 老师列表 + 已修/在修课程(enrollment_state=active+completed)、公告、权重、文件名；讲师背景只来自学生手动填的 https *.nus.edu.sg 教师主页(重定向也限制域名)。写作风格只从应用内笔记学(≥400 字，可关，关掉即删除)。LLM 用学生自己的 key(`kitFor`)，输入哈希不变不重建，每轮最多 2 个模块画像；代码层强制：没评论就没有 studentsSay、没主页就没有讲师 background、不在课程里的讲师丢掉。UI：模块页顶部 Module profile 卡片(含本周重点)；Account 顶部 About you；首页 This week。Rebuild 按钮 → `/api/profile` 置 requested，worker 每 2s tick 处理，一次尝试后必清除。指南生成用 `readerBrief` 注入读者画像。验证：云端 420 单测、build、真 worker + mock LLM 端到端(真实 NUSMods 拉取 AY2026-2027 成功)、Playwright(415/404、非 NUS 主页被拒、390px 无溢出)。线上尚未有真实模型跑过。
- 09-23 (Cowork)：**所有课程文件在应用内打开**，commit `b26b4aa` → **v29 complete**。Materials 行改链到 `/modules/[id]/files/[fileId]`(左侧文件列表可过滤、j/k 切换；手机上是下拉)。PDF 用浏览器端 PDF.js(pdfjs-dist 5.4.296 显式依赖，`prebuild/predev` 把 worker/cmaps/standard_fonts/wasm 复制到 `public/pdfjs/`，已 gitignore)：Range 分块加载、只画附近页、可选中文字、记住页码(localStorage `file-page:<mod>:<file>`)。pptx/docx/xlsx 服务端 LibreOffice 转 PDF(镜像新增 libreoffice-impress/writer/calc + 字体含 noto-cjk，镜像变大约 1.1GB 包；一次只转一个，180s 超时)，结果写进 guide 同一个 deck-cache `<stem>.pdf`，所以 pptx 课程的指南引用也能用。非 PDF 原件缓存在 `<dbdir>/file-cache/<courseId>/<canvasFileId>`。路由 `/api/modules/[id]/files/[fileId]`(`?as=pdf`、`?download=1`)：Range/ETag/private 缓存、sandbox CSP，html/svg 一律 attachment。下一个 PDF/office 文件后台预热。验证：云端 397 单测、build、Playwright(30 页 PDF 首帧 ~0.7s、150 页跳到 120 页只画 13 页、12 页 pptx 首次转换 1.7s 后续 0.3s、CSV 表格、图片、zip 下载、html 头、Range 206、匿名 401、390px 无横向溢出)。线上只验证了 soffice 25.8 存在、路由已挂、需要登录；**没在线上用真实 Canvas 文件点过**。风险：1GB 机器上转大 pptx 可能吃 300MB+。
- 09-23 (Cowork)：**账号页 /account(左栏新图标，外观上方)**，commit `2a3d6c2` → **v28 complete**。每个用户存自己的 AI provider(OpenAI 兼容：OpenAI/Anthropic/Gemini/OpenRouter/自定义)，新列 `llm_base_url/llm_model/llm_key_enc`(迁移 0015，SECRET_KEY 加密)，保存前真调一次 chat/completions，key 只写不读(前端只见后 4 位)；换 base URL 必须重输 key。worker 改为按用户 `kitFor(userId)` 选 provider，没设就用共享 env key(**注意：线上 secrets 里根本没有 OPENAI_COMPAT_*/ANTHROPIC_API_KEY，所以线上生成指南现在只能靠用户自己的 key**)。Canvas：已登录可直接换 token(必须同一 canvas_user_id)，worker 遇 Canvas 401 写 `canvas_token_failed_at`，首页黄条引导去 Account；新列 `canvas_verified_at`。登录"First time"成功后跳 `/account?welcome=1`。账号页还能设置/修改用户名密码(改密需旧密码)。account 路由只收 JSON(防 text/plain 跨站)、每用户 20 次/10 分钟。api.openai.com 用 `max_completion_tokens` 且不传 temperature。验证：云端 clone 375 单测、next build、Playwright 走了坏 key(真实 OpenAI 401 文案)、坏 Canvas token、私网 URL/http 拒绝、415、改密错旧密码、无 key 时指南页变"Add an AI key"链接且 POST 409、深色、390px 无横向滚动。成功保存路径只有单测覆盖(没有真 key)。
- 09-22 (本次)：**命名统一为 OpenPapr**。可发布物本来就已经是 openpapr(package/fly app/volume/DATABASE_PATH/remote/README/src)，本次只清了文档与状态文件：`docs/superpowers/` 的 spec 标题+plan 标题/正文、plan 里已过时的配置字面量(`data/one-ring.db`→`openpapr.db`、`one_ring_data`→`openpapr_data`、`app="openpapr"`、fly 域名、worker 日志)对齐线上实际值；`docs/spike/RESULTS.md`、`STATE.md`、`.claude/PROJECT-HISTORY.md` 标题。**故意保留**的 one-ring：真实目录路径、plan 里的目录树、Entra 应用注册名 `one-ring`(Azure 里就叫这个)、PROJECT-HISTORY 里的 transcript 路径 —— 这些是事实不是品牌。目录本身没改名(改了会把 `~/.claude/projects/-Users-aiden-Desktop-Flow-one-ring/` 的 memory 和两份 transcript 变孤儿)。memory 侧：用户级 `one-ring-project.md`→`openpapr-project.md`(连带 MEMORY.md / canvas-nus-pullable-map / index-archive 的引用)；项目级 `project_cheatsheetmaker.md` 已删 —— 它描述的是 A4 便条纸生成器那套(Postgres/Hono/Puppeteer/monorepo + CHEATSHEETMAKER.md)，本仓早就不是了，换成 `project_openpapr.md` 记改名史+pivot。
- 09-22 (Cowork 会话)：**UI 重设计已提交并上线**：commit `f6274a4` 已 push 到 origin/main；`fly deploy` 后机器 `865d47bee4d508` 跑新镜像 `deployment-01M345YGXXN9X4JSCV68ESENMP`(09:09 UTC)，/login 已出新样式、worker 正常起。注意 `fly releases` 里 v19 标 "interrupted"——是我误起了两个并行 deploy 后 kill 掉一个所致，机器本身是新版本；随后 `8a58407`(Materials 改 CSS 多栏流式+同名同大小去重；公告长 URL 撑宽页面修掉：grid 列 minmax(0,3fr)+overflow-wrap:anywhere) 已 push 并部署为干净的 v20；再 `e64487d`(公告改成 32rem 高的内滚动框，去掉 Older 折叠) → v21；再 `9ee4e94`(指南右侧幻灯片面板 + 按页笔记：新表 `slide_notes` 迁移 0014，路由 `/api/modules/[id]/notes` 与 `/deck/info`；点引用 chip 在面板里打开，修饰键点击仍开新标签页) → v23；再 `9d89cf3`(面板缩略图条带 note/cited 标记、笔记三种范围 This slide/This deck/Module、<1280px 改为右侧抽屉含手机、Esc 关闭、修 "+" 下拉被裁；服务端把渲染好的页 PNG 缓存到卷上 `<stem>-pages/<page>@<scale>.png`，`/slide?size=thumb`) → v24；再 `e9d4f5a`(指南上方工具条 Chapters/Outline/Slides 开关、面板关闭时右下角浮动 "Slides" 按钮、面板左缘可拖动调宽 360–960px、面板视图 Slide/Both/Notes、"Pages" 隐藏缩略图；布局偏好存 localStorage `sg-layout`；手机上补了章节下拉) → v25；再 `d7112f3`(按钮位置固定：页面顶部一条吸顶阅读栏放章节下拉 + Chapters/Outline/Slides，放不下时禁用而不是隐藏；去掉浮动 Slides 按钮；面板两行固定：页码/视图/关闭，文件标签/+/缩略图/PDF；各侧栏 sticky top 改 72px) → v26；再 `95da157`(深色模式 + 6 种强调色：左侧栏退出按钮上方的 Appearance 弹层，Light/Dark/System；`<html data-theme data-accent>`，head 内联脚本首帧前设置防闪白；偏好存 localStorage `op-theme`/`op-accent`；所有颜色走 token，深浅两套均过 AA) → **v27 complete**。已知遗留：指南页在 390px 宽有 19px 横向滚动(与面板无关，面板关闭时也有)。改动=`src/app/globals.css`(ink-2/ink-3 调深到 AA、新 token accent-soft/warn-*、guide 版心 680px)、`Rail`(56px、44px 目标、usePathname 高亮)、`AppShell`、首页(`DueThisWeek` + `WeightBar` 权重条瓷砖)、模块页(24px 标题、主按钮"Open study guide"、`SourceChip` 内联证据、Materials 分组两栏)、指南页(编号章节、引用 chip 强调色、图注"Open deck, slide N →")、`lib/format-date.ts` 统一日期。设计稿在 Claude Design 画布 "OpenPapr UI review"。
- 验证：本机 `tsc` 通过；vitest/next build/playwright 在本机 VM 跑不了(node_modules 是 darwin 二进制)，改在云端 clone 同一 HEAD 打补丁跑过：335 单测通过、`next build` 通过、5 个 e2e smoke 通过。
- 09-11 会话 `9f982086`：合并后的 `main` 已部署到 Fly，滚动发布，机器健康 —— web 08:10:16 UTC ready，worker 晚 5 秒起，之后日志无错误。
- 同日 `b04ac567`(119 轮，压缩过 2 次)：修好登录后立刻掉线的问题。

## 在跑的东西 / 端点
- 线上 **openpapr.fly.dev**，可用 `mjykxz17` 登录(无需邀请码或 Canvas token)。
- 公开仓库 github.com/mjykxz17/OpenPapr；本地 `~/Desktop/Flow/one-ring`。

## 最近结论
- 掉线真因：session cookie **没有 max-age** —— 浏览器当成会话 cookie，关掉浏览器就丢，尽管里面签名 token 有效期 30 天。
- 路由验证：`/login` 200；`/modules/1`、`/modules/1/guide`、`/study` 匿名访问均重定向到登录。
- Canvas 可拉取全图在 `docs/spike/canvas-pullable-map-2026-09-03.md`：成绩是**被隐藏**而非不存在(graded_submissions 有 321 条历史分)；LTI 边界(Zoom/Panopto/New Quizzes)拉不到。

## Blocker
- 🔴 历史教训：fixtures 曾含真实凭据，已用 filter-repo 清除 —— 提交前检查测试数据。

## 下一步
- 线上过一眼真实数据(尤其 ModuleGrid 3 列 + 权重条、指南 chip)；`ModuleCard.tsx` 已无引用，可删。CLAUDE.md 本地改动未提交(本目录规矩)。
- `[待确认]` 是否要把 mail 页 / reminders 页也按新字号体系再走一遍(目前只改了标题层级)。

## 来源
`.claude/PROJECT-HISTORY.md`(2 个会话) / memory: `openpapr-project` `canvas-nus-pullable-map` / 本仓 README、CLAUDE.md
- 09-25：首页 This week 卡片精简（`23299e2`，已 push）：今日清单最多 3 条、重点改成模块 chip（原因悬停显示）、其余天数折叠成一行；PLAN_SYSTEM 限制概述一句、≤4 重点、每天 ≤3 条短任务；旧计划在视图里截断；plan hash 加 v:2 触发重建。**未部署**：Mac 上 flyctl 登录失效（no access token），需 `fly auth login` 后 `fly deploy`。
- 09-25：**智能 Tasks**（`c6e885a`，已部署为 v38，09-25 06:28 UTC，health ok）：新表 tasks/file_hints/task_plans（迁移 0020）；worker `refreshTasks` 每模块一次 LLM 调用（src/enrich/tasks.ts 的 PLANNER_SYSTEM），合并 Canvas/公告/课件提示为同一任务、预判下一次、拆成带 doBy 的步骤；勾选过的步骤重规划时保留、错过的步骤顺延、每日上限 3h/4h；Canvas 提交后自动完成；只凭权重的任务不给日期；/reminders 重定向到 /tasks；首页 This week 改为摘要+今日步骤链接；fly.toml 加 TZ=Asia/Singapore（服务端时间此前按 UTC 显示）。用旧本地库真实跑过 agnes-2.5-flash：偶发空回复已加一次重试，失败 30 分钟退避。
- 09-25：Canvas 同步节奏（`5e1afd3`，已部署 v39）：白天每 5 分钟（POLL_INTERVAL_MS 默认），新加坡时间 01:00–07:00 每小时；打开首页时若上次 Canvas 同步超过 2 分钟则立即同步；stale 判定按夜间节奏放宽到结束后 1 小时（src/lib/poll-schedule.ts）。
- 09-25：讨论区 + Canvas Planner 融合（`225dd3d`，已部署）：items 新类型 discussion / staff_reply / planner_note，新列 meta_json / missing / canvas_done（迁移 0021）。只存老师/助教回帖正文，同学回帖只计数。模块页 Announcements 改为 Updates（公告+讨论按最新活动排序，讨论内嵌老师回复、未读数、"还没发帖"）。任务规划读入老师回复、需发帖的讨论、Planner 笔记、Missing 标记；发帖/Canvas 打勾/提交即完成；安全网：两周内到期却无任务引用的 Canvas 项自动生成任务（key 含 -canvas-，规划覆盖后才删除）。missing_submissions 用 filter[]=submittable。
